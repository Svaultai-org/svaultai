name: vault_ai_frontend
description: Vault AI front-end (Email OTP → PIN → Video → Chat)
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ">=3.4.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.1.5
  shared_preferences: ^2.3.2
  cupertino_icons: ^1.0.6
  url_launcher: ^6.3.2
  descope: ^0.9.9

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
